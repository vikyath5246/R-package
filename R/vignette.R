# library(devtools)
# use_vignette("ensemble_regression", title="Ensemble learning")
# Build : install and restart
#
# Console:
#   library(SimpleEnsembleGroup17)
# help(package=“SimpleEnsembleGroup17”)
# ?install_github #one way using git
# #other way in our local is:
# More >  Build Source Package
# Packages > unload
#
# install(down)
# Install form: archive package > tar file > install
